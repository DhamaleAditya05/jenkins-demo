pipeline {
    agent any

    stages {
        stage('Clone Info') {
            steps {
                sh 'pwd'
                sh 'ls -la'
            }
        }

        stage('Build') {
            steps {
                echo 'Building project...'
            }
        }
    }
}
